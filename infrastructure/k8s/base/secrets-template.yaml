# DO NOT commit real secrets. This is a template only.
# Real secrets are injected via External Secrets Operator from AWS Secrets Manager.
apiVersion: v1
kind: Secret
metadata:
  name: supporthub-db-secret
  annotations:
    secret-template: "true"
type: Opaque
stringData:
  url: "PLACEHOLDER_DB_URL"
  username: "PLACEHOLDER_DB_USER"
  password: "PLACEHOLDER_DB_PASS"
---
apiVersion: v1
kind: Secret
metadata:
  name: supporthub-api-keys
  annotations:
    secret-template: "true"
type: Opaque
stringData:
  anthropic-api-key: "PLACEHOLDER"
  msg91-api-key: "PLACEHOLDER"
  sendgrid-api-key: "PLACEHOLDER"
  encryption-secret-key: "PLACEHOLDER"
